# The deployed frontend is hosted separately from the Express API. Vite embeds
# this value at build time, so the catalog requests reach the backend instead
# of the static site's non-existent /api route.
VITE_API_URL=https://ai-auramart.onrender.com
