# Local development uses the /api proxy configured in vite.config.js.
# For a separately deployed frontend, set this to the backend origin (no trailing slash).
VITE_API_URL=https://ai-auramart.onrender.com
